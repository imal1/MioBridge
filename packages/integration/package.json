{
  "name": "@miobridge/integration",
  "version": "1.2.12",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run --cwd ../cli build && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@miobridge/cli": "workspace:*",
    "@types/node": "^24.0.8",
    "ajv": "^8.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  }
}
